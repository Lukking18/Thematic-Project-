const search = (req, res) => {
    return res.sendStatus(500);
}

const get_movie = (req, res) => {
    return res.sendStatus(500);
}

module.exports = {
    search: search,
    get_movie: get_movie
}