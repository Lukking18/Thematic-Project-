# Thematic-Project-

Run `npm install` to install all dependencies for the project
 Once the dependencies have installed, run the server using `npm run dev`. You will see a message saying that the server is running on port 3333.
 Press ctrl + C to restart the server